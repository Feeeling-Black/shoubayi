# 手把一联机原型部署说明

当前版本适合先给家人远程试玩：四个人用同一个公网地址进入，创建房间后分享房号。

## 本地启动

```bash
npm start
```

打开：

```text
http://localhost:4180
```

健康检查：

```text
http://localhost:4180/health
```

## 公网部署

推荐先用支持 Node/Docker 的平台，例如 Render、Railway、Fly.io 或一台云服务器。

部署时使用：

```bash
npm start
```

服务端口读取环境变量 `PORT`，没有设置时默认 `4180`。

## 现在的限制

- 房间数据暂存在服务器内存里，服务重启后房间会消失。
- 先按单实例部署，不要开启多实例扩容。
- 这是联机原型，适合先验证远程四人完整打一局。

## 后续上微信小程序

这套房间、发牌、出牌同步接口可以继续复用。小程序阶段主要替换前端入口，并补微信登录、分享房号、断线重连和房间持久化。
